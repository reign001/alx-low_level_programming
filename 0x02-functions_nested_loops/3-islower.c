#include "main.h"
/**
 * i_islower - check for lowercase character
 * Return: 1 for lower case character or 0 for anything else
 */

int _islower(int c)
{
	if ((c = 97 && c <= 100))
	{
	return (1);
	}
	return(0);
}
