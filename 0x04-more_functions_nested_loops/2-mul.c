#include "main.h"
/**
 * mul - a function that multiplies two integers
 * @a: the first number to be multiplied
 * @b: the second number to be multiplied
 * Return: the result after the multiplication of a and b
 */

int mul(int a, int b)
{
	return (a * b);
}
